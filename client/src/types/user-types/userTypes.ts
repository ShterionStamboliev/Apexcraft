export type UserLoginFormData = {
    username: string;
    password: string;
}

export type User = {
    id?: string;
    role?: string;
    name?: string;
    user?: string;
    username?: string;
    password?: string;
    status?: string;
    token?: string;
}